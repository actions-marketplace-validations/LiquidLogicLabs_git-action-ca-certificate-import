# .devcontainer-secrets.sample
#
# Copy this file to .devcontainer-secrets in the project root and fill in your values.
# .devcontainer-secrets is gitignored — never commit it.
#
# This file is automatically copied to .devcontainer-secrets the first time you
# open the project in a devcontainer (via initializeCommand in devcontainer.json).
#
# Supports full bash expansion: ${VAR}, ${VAR:-default}, $(command), etc.

# GitHub Personal Access Token — used by git push (HTTPS) and the gh CLI.
# Scopes needed: repo (for private repos), read:packages (for GitHub Packages).
# Create at: https://github.com/settings/tokens
export GITHUB_TOKEN=${GITHUB_TOKEN}

# Gitea access token — used for self-hosted Gitea / Forgejo instances.
# Leave empty if not using a self-hosted git host.
export GITEA_TOKEN=${GITEA_TOKEN}

# NPM registry authentication token — used for private package registries.
# Leave empty if using only the public npm registry.
export NPM_REGISTRY_TOKEN=${NPM_REGISTRY_TOKEN}

# Docker Hub registry authentication token - used for image pushes
# Leave empty if not using
export DOCKERHUB_TOKEN=${DOCKERHUB_TOKEN}
